apiVersion: v1
kind: Service
metadata:
  name: flask-test
  labels:
    app: flask-test
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 8080
  selector:
    app: flask-test
