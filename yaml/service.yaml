apiVersion: v1
kind: Service
metadata:
  name: flask-test1
  labels:
    app: flask-test1
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30000
  selector:
    app: flask-test1
